server:
  port: 8090

spring:
  application:
    name: config

  profiles:
    active:
      - native #diğer uygulamaların config ayarlarını config dizininden import edilmesini sağlar

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8097/eureka  # !!! eureka nin adresini yaziyoruz

